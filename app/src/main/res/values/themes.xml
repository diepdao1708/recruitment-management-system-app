<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Resume" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSurfaceVariant">@color/ripple</item>
        <item name="colorOnSurface">@color/primary</item>

        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Resume" parent="Base.Theme.Resume" />
</resources>